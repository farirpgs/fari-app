import { createMuiTheme } from "@material-ui/core";
import { blue, deepOrange } from "@material-ui/core/colors";
export const theme = createMuiTheme({
  palette: { primary: blue, secondary: deepOrange }
});
